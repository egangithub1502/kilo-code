apiVersion: v1
kind: Service
metadata:
  name: stratex-preview-svc
  namespace: profit
  labels:
    app: stratex
    type: preview
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: stratex
      #rollout: preview
  type: ClusterIP

